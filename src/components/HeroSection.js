// src/components/HeroSection.js
import React from 'react';
import './HeroSection.css'; // You can add a CSS file for this component later

function HeroSection() {
  return (
    <section className="hero-section">
      {/* This heading will be dynamic later, but for now it's static */}
      <h1>Find & Share Community Events in Bhubaneswar</h1>
    </section>
  );
}

export default HeroSection;